/**
 * Test Utilities
 * 
 * Centralized test helpers, mock factories, and utilities.
 * Import from '@/test-utils' for cleaner test files.
 * 
 * @example
 * import { createMockTeam } from '@/test-utils';
 */

export { createMockTeam, createMockTeamDriver } from './mockFactories';
